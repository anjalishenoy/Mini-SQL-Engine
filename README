This is a simple mini-SQL engine

to run the engine,type
$ bash 201401114.sh

it works on the two tables given in the 'metadata' folder.

The engine handles 
-queries having column names (A,B.. or table1.A, table2.B..) and aggregate functions such as sum, average, min and max distinct only.
-join on the 2 table given any condition
-handles where queries with <,<=, >,>=, <> 

parser was made from scratch
error handling and syntax handling has been done
"group by" was not asked to be implemented
~500 lines of code
